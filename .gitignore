node_modules
.next
out
build
dist

.env
.env.local
.env*.local

.DS_Store
*.log
.vercel
.turbo

next-env.d.ts
tsconfig.tsbuildinfo

# Supabase CLI local link artifacts (project-ref, pooler-url 등) — 커밋 금지
supabase/.temp/

# pdfjs worker (postinstall이 pdfjs-dist에서 복사)
/public/pdf.worker.min.mjs

# Claude Code — 이 머신에만 해당하는 것만 제외한다.
# settings.local.json: 로컬 권한 허용목록 (공유용은 settings.json → 커밋 대상)
# worktrees/: git worktree 사본, node_modules 포함해 수백 MB
.claude/settings.local.json
.claude/worktrees/
